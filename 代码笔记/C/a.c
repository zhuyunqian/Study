#include <stdio.h>

int main(){
    int a1 =22;
    int b1 = 33;
    int maxNum = max(a1,b1);
    int minNum = min(a1,b1);
    printf("that maxnumber is %d that minbunber is %d",maxNum,minNum);
    return 0;
}
